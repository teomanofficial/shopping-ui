import { EntityStateModel } from '@core/store';
import { ProductResponseModel } from '@store/products/models/product-response.model';

export interface ProductStateModel extends EntityStateModel<ProductResponseModel> {
}
