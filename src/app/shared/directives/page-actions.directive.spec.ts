import { PageActionsDirective } from './page-actions.directive';

describe('PageActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new PageActionsDirective();
    expect(directive).toBeTruthy();
  });
});
